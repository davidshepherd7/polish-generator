{
  "name": "polish-grammar",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "build": "tsc && browserify dist/index.ui.js -o bundle.js",
    "prestart": "npm run build",
    "start": "node dist/index.node.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Shepherd",
  "license": "ISC",
  "devDependencies": {
    "@types/lodash": "^4.14.104",
    "browserify": "^16.1.1",
    "jasmine": "^3.1.0",
    "jasmine-ts": "^0.4.0",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
